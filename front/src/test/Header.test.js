import Header from '../components/Header.js'

describe("Header", () => {

  describe("when it's just a browser", () => {
    it("redirects to an FAQ page", () => {});
  });

  describe("when Donor has EVC", () => {
    it("shows the Donor's organization", () => {});
    it("shows the Donor's balance", () => {});
  });

});

